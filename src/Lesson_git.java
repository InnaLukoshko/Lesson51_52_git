public class Lesson_git {
    public static void main(String[] args) {
        sum(2,4);
        mult(1,3); // вызов функции умножения
   }
   private static int sum(int x, int y) {
        return x + y;
   }

    private static int mult(int x, int y) {
        return x * y;
    }
}

